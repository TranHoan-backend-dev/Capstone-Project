spring:
  application:
    name: image
  jackson:
    time-zone: Asia/Ho_Chi_Minh
    locale: vi_VN
  profiles:
    active: dev
management:
  health:
    rabbit:
      enabled: false
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
          - metrics
          - prometheus
      base-path: /actuator
server:
  port: 9009
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
      force-request: true
      force-response: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka
  instance:
    prefer-ip-address: true
#spring:
#  cloud:
#    gcp:
#      credentials:
#        location: classpath:${gcs.credentials}
#gcs:
#  credentials: gcs/project-b0d9c9e8-e136-4e83-97e-732cf573e95c.json
#  bucket: nawaco
